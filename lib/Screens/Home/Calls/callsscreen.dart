import 'package:flutter/material.dart';

class Callsscreen extends StatelessWidget {
  const Callsscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Calls screen")));
  }
}
